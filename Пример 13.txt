
&НаСервереБезКонтекста
Процедура СоздатьНаСервере(ДанныеСФормы)

	//1	СоздатьЭмент()
	//2 Редактирование реквезитов
	//3 Записать()
	
	НоваяДолжность = Справочники.Должности.СоздатьЭлемент();
	НоваяДолжность.Наименование = ДанныеСФормы.Наименование;
	НоваяДолжность.Записать();
КонецПроцедуры

&НаКлиенте
Процедура Создать(Команда)
	
	ДанныеСФормы = Новый Структура;
	ДанныеСФормы.Вставить("Наименование", Наименование);
	ДанныеСФормы.Вставить("МаксимальныйОклад", МаксимальныйОклад);
	ДанныеСФормы.Вставить("МинимальныйОклад", МинимальныйОклад);
	ДанныеСФормы.Вставить("Категория", Категория);
	
	СоздатьНаСервере(ДанныеСФормы);
КонецПроцедуры

&НаСервереБезКонтекста
Функция НайтиНаСервере(НаимДолжн)
	
	НайденныеДанныеССервера = Новый Структура;
	НайденныйЭлемент = Справочники.Должности.НайтиПоНаименованию(НаимДолжн);
	НайденныеДанныеССервера.Вставить("МаксимальныйОклад", НайденныйЭлемент.МаксимальныйОклад);
	НайденныеДанныеССервера.Вставить("МинимальныйОклад", НайденныйЭлемент.МинимальныйОклад);
	Возврат НайденныеДанныеССервера;
КонецФункции

&НаКлиенте
Процедура НайтиДолжность(Команда)
	ДанныеССервера = НайтиНаСервере(Наименование);
	
	МаксимальныйОклад = ДанныеССервера.МаксимальныйОклад;
	МинимальныйОклад = ДанныеССервера.МинимальныйОклад;
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура УдалитьДолжностьНаСервере(Наименование)
	Переме = Справочники.Должности.НайтиПоНаименованию(Наименование).ПолучитьОбъект();
	Переме.Удалить();
КонецПроцедуры

&НаКлиенте
Процедура УдалитьДолжность(Команда)
	УдалитьДолжностьНаСервере(Наименование);
КонецПроцедуры

&НаСервереБезКонтекста
Процедура СохранитьДНаСервере(ДанныеСФормы)

	СохраняемаяДолжность = Справочники.Должности.НайтиПоНаименованию(ДанныеСФормы.Наименование).ПолучитьОбъект();
	СохраняемаяДолжность.МинимальныйОклад = ДанныеСФормы.МинимальныйОклад;
	СохраняемаяДолжность.Записать();
КонецПроцедуры

&НаКлиенте
Процедура СохранитьД(Команда)
	
	ДанныеСФормы = Новый Структура;
	ДанныеСФормы.Вставить("Наименование", Наименование);
	ДанныеСФормы.Вставить("МаксимальныйОклад", МаксимальныйОклад);
	ДанныеСФормы.Вставить("МинимальныйОклад", МинимальныйОклад);
	ДанныеСФормы.Вставить("Категория", Категория);
	СохранитьДНаСервере(ДанныеСФормы);
	
КонецПроцедуры
